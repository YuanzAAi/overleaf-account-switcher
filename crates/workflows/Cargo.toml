[package]
name = "overleaf-workflows"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "overleaf_workflows"
path = "src/lib.rs"

[dependencies]
overleaf-core = { path = "../core" }
overleaf-storage = { path = "../storage" }
